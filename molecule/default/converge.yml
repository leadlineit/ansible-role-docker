---
- name: Converge
  hosts: all
  tasks:
    - name: install requirements
      apt:
        name: gnupg
        state: present
        update_cache: true
    - name: "Include leadlineit.docker"
      include_role:
        name: "leadlineit.docker"
